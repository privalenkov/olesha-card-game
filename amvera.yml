meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build

run:
  command: NODE_ENV=production npm run start
  persistenceMount: /data
  containerPort: 8787
